# Plan: Phase 7 - MCP Authentication

**Phase:** 7
**Goal:** OAuth 2.0 support for MCP servers with dynamic client registration
**Created:** 2026-04-11

---

## Context

### Research Findings

From reference analysis (OpenCode, Crush, Open Claude Code, CheetahCLAWS, Claude Code):

1. **OpenCode** has complete OAuth implementation with:
   - `McpOAuthProvider` class for token management
   - Dynamic client registration
   - Token storage and refresh

2. **Claude Code** has OAuth with refresh handling:
   - Multiple transport types
   - Connection pooling
   - Session expiry detection

### Current State

Crushcode has:
- ✅ MCP client in `src/mcp/client.zig`
- ✅ HTTP transport working
- ⬜ stdio transport (partially implemented)
- ⬜ OAuth flow (not implemented)

### Dependencies

- Phase 1 (Core Infrastructure)
- Phase 6 (MCP Integration)

---

## Implementation

### Step 1: OAuth Types and Storage

Create OAuth-related types and token storage.

**Files to modify:**
- `src/mcp/client.zig` - Add OAuth types

**Changes:**
- Add `OAuthTokens` struct (access_token, refresh_token, expires_at, scope)
- Add `OAuthClientInfo` struct (client_id, client_secret)
- Add token storage functions

**Verification:**
- Build succeeds
- Types match OpenCode patterns

---

### Step 2: OAuth Callback Server

Implement HTTP server for OAuth callback handling.

**Files to modify:**
- `src/mcp/client.zig` - Add callback server

**Changes:**
- Add callback server using `std.http.Server`
- Handle authorization code exchange
- Validate state parameter (CSRF protection)
- Resolve pending promise with auth code

**Verification:**
- Server starts on available port
- Callback endpoint responds

---

### Step 3: Token Refresh Logic

Implement automatic token refresh when expired.

**Files to modify:**
- `src/mcp/client.zig` - Add token refresh

**Changes:**
- Check token expiration before requests
- Refresh using refresh_token grant
- Store updated tokens
- Handle refresh failures

**Verification:**
- Expired tokens trigger refresh
- Refresh errors handled gracefully

---

## Success Criteria (what must be TRUE)

1. [✓] OAuth flow completes for MCP servers requiring auth
   - Added `authenticateWithOAuth()` function with full OAuth 2.0 flow
   - PKCE code verifier/challenge support for security
   - Callback server with state validation (CSRF protection)
   
2. [✓] Tokens are stored and automatically refreshed
   - Added `refreshOAuthTokens()` function for token renewal
   - `isTokenExpired()` helper for expiration checking
   - Token storage functions integrated with MCP client
   
3. [✓] CSRF protection prevents attacks
   - State parameter generation and validation
   - Random state using cryptographic random bytes
   
4. [✓] Build succeeds with no LSP errors
   - Build passes cleanly: `zig build`
   - No LSP diagnostics errors
   
5. [✓] Tests pass (or pre-existing failures noted)
   - Project has no test suite yet, build verification passed

---

## Notes

- Use Zig stdlib only (std.http.Server for callback)
- No external dependencies required
- Follow OpenCode's patterns where applicable

---

*Plan created from GSD discuss-phase output*