# Plan: Phase 9 - Skill System

**Phase:** 9
**Goal:** SKILL.md parsing and runtime skill loading
**Created:** 2026-04-11

---

## Context

### Research Findings

From reference analysis:

1. **Crush** has complete skill system:
   - YAML frontmatter parsing in `skills.go`
   - Directory discovery with fastwalk
   - XML generation for prompts

2. **CheetahCLAWS** has skill loader:
   - Markdown + YAML frontmatter parsing
   - Skill discovery and validation

3. **Claude Code** has skill system:
   - Reusable workflow definitions
   - Dynamic loading

### Current State

Crushcode has:
- ⬜ Skill system (not implemented)

### Dependencies

- Phase 1 (Core Infrastructure)

---

## Implementation

### Step 1: Skill Types

Define skill types and parsing logic.

**Files to create:**
- `src/skills/types.zig` - Skill definitions
- `src/skills/parser.zig` - YAML parsing

**Changes:**
- Define `Skill` struct (name, triggers, tools, prompt)
- Parse YAML frontmatter from SKILL.md
- Split frontmatter from markdown body

**Verification:**
- Build succeeds
- Parsing works for sample SKILL.md

---

### Step 2: Skill Discovery

Implement directory walking for skill discovery.

**Files to modify:**
- `src/skills/loader.zig` - New file

**Changes:**
- Use `std.fs.Dir.walk()` for directory traversal
- Filter for SKILL.md files
- Validate discovered skills

**Verification:**
- Skills discovered from directory
- Invalid files handled gracefully

---

### Step 3: Prompt Integration

Generate XML for AI prompts.

**Files to modify:**
- `src/skills/loader.zig` - Add XML generation

**Changes:**
- Implement `ToPromptXML()` function
- Format skills as `<available_skills>` XML
- Inject into system prompts

**Verification:**
- XML output matches expected format
- AI receives skill information

---

## Success Criteria (what must be TRUE)

1. [ ] Skills are loaded from SKILL.md files
2. [ ] Skills appear in AI prompts
3. [ ] User skills override builtins
4. [ ] Build succeeds with no LSP errors

---

## Notes

- Use Zig stdlib only (std.fs, std.json)
- YAML parsing may need manual implementation or JSON conversion

---

*Plan created from GSD discuss-phase output*