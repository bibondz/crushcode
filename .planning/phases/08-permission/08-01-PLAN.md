# Plan: Phase 8 - Permission System

**Phase:** 8
**Goal:** Pattern-based permission allow/deny/ask rules for tool safety
**Created:** 2026-04-11

---

## Context

### Research Findings

From reference analysis:

1. **OpenCode** has pattern-based permissions:
   - `evaluate()` function with pattern matching
   - Actions: "allow", "deny", "ask"
   - Wildcards supported

2. **Crush** has allowlist-based permissions:
   - "tool:action" format
   - Session auto-approval
   - Pubsub for user prompts

3. **Claude Code** has granular tool permissions:
   - Pattern-based rule matching
   - Interactive permission prompts

### Current State

Crushcode has:
- ⬜ Permission system (not implemented)

### Dependencies

- Phase 1 (Core Infrastructure)

---

## Implementation

### Step 1: Permission Types

Create permission types and evaluation logic.

**Files to create:**
- `src/permission/types.zig` - Permission types
- `src/permission/evaluate.zig` - Pattern matching

**Changes:**
- Define `PermissionAction` enum (allow, deny, ask)
- Define `PermissionRule` union type
- Implement `evaluate()` function with wildcard support

**Verification:**
- Build succeeds
- Pattern matching works for simple cases

---

### Step 2: User Prompts

Implement user prompt for "ask" actions.

**Files to modify:**
- `src/permission/prompt.zig` - New file

**Changes:**
- Add prompt function using stdio
- Handle timeout for prompts
- Support session auto-approval

**Verification:**
- Prompt displays and accepts input
- Timeout works correctly

---

## Success Criteria (what must be TRUE)

1. [✓] Tools can be allowed/denied by pattern
   - Implemented `PermissionEvaluator` with OpenCode-style pattern matching
   - Wildcard support (* and ?) with last-match-wins rule
   - Tool:action format (Crush pattern) with permission_id generation
   
2. [✓] User is prompted for confirmation when needed
   - Created `PromptHandler` with Claude Code-style readable prompts
   - Response options: once, always, reject, always_reject
   - Timeout handling with non-blocking input
   
3. [✓] Auto-approval works for trusted operations
   - Session auto-approval (Crush pattern) with session ID tracking
   - Operation auto-approval (OpenCode pattern) for persistent rules
   - Permission modes including bypassPermissions, auto, plan modes
   
4. [✓] Build succeeds with no LSP errors
   - All 4 permission modules compile cleanly
   - Integration with existing codebase successful
   - Security checks pass validation

---

## Notes

- Use Zig stdlib only
- Follow OpenCode's evaluate() pattern

---

*Plan created from GSD discuss-phase output*