# Phase 11 Plan — LSP Deep Integration

**Phase:** 11 (v0.5.0)
**Goal:** LSP diagnostics integrated into TUI sidebar with auto-detection

---

## Task Breakdown

### Task 1: Add LSP module import to TUI in build.zig
- Add `imp("lsp", lsp_mod)` to `tui_mod` imports in `build.zig`
- Find correct location (after existing tui_mod imports around line 325)

### Task 2: Add LSP imports and types to chat_tui_app.zig
- Add `const lsp = @import("lsp");` import
- Add `DiagnosticSummary` and `DiagnosticInfo` type definitions
- Add `lsp_clients: std.ArrayList(LspEntry)` field to Model
- Add `lsp_diagnostic_summary: ?DiagnosticSummary` field to Model

### Task 3: Implement lazy LSP client initialization
- Add `LspEntry` struct helper
- Add helper function to get or create LSP client for a language
- Initialize `lsp_clients` in Model.init()
- Add cleanup in Model.destroy()

### Task 4: Hook LSP into executeToolCalls
- After write/edit tool execution, detect file language
- Lazy-init LSP client if needed
- Call openDocument() with updated content
- Call getDiagnostics() and cache results

### Task 5: Add diagnostic drain in draw loop
- In Model.draw(), before rendering sidebar
- Drain LSP notifications (non-blocking)
- Update diagnostic cache

### Task 6: Add Diagnostics section to SidebarWidget
- Add "Diagnostics" section between Workers and Theme
- Show error/warning counts with severity colors
- Show top 3 diagnostics with file:line - message
- Handle zero state and no LSP state

### Task 7: Add /diag palette command
- Add to palette_command_data array
- Show full diagnostics as assistant message

---

## Execution Order

Parallel: Tasks 1, 2 (independent)
Sequential: Tasks 3-7 (depend on previous)

---

## Verification

- Build passes: `zig build`
- No new LSP features crash when no LSP installed
- Sidebar shows diagnostics when LSP active
