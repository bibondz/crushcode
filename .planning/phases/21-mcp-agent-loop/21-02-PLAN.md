---
phase: 21-mcp-agent-loop
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/tui/chat_tui_app.zig
  - src/tui/widgets/sidebar.zig
autonomous: true
requirements: [MCP-02, MCP-03]

must_haves:
  truths:
    - "User can see which MCP servers are connected in sidebar"
    - "MCP servers auto-connect on TUI startup"
    - "MCP server status updates (connected/disconnected) visible in real-time"
  artifacts:
    - path: "src/tui/widgets/sidebar.zig"
      provides: "MCP server status section in sidebar"
      contains: "MCP"
    - path: "src/tui/chat_tui_app.zig"
      provides: "MCP server lifecycle management"
      contains: "connectAll|mcp_server_status"
  key_links:
    - from: "src/tui/widgets/sidebar.zig"
      to: "src/mcp/bridge.zig"
      via: "Reads server status from bridge"
      pattern: "mcp_bridge|server_status"
---

<objective>
Add MCP server lifecycle management (auto-discover, connect, health-check) and show server status in the TUI sidebar.

Purpose: Users need visibility into MCP server state — which servers are available, connected, and how many tools they provide.
Output: Sidebar section showing MCP server status + auto-connect on startup.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mcp-agent-loop/21-CONTEXT.md
@.planning/phases/21-mcp-agent-loop/21-01-SUMMARY.md
@src/tui/widgets/sidebar.zig
@src/tui/chat_tui_app.zig
@src/mcp/bridge.zig
@src/mcp/discovery.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP Server Status in Sidebar</name>
  <files>src/tui/widgets/sidebar.zig</files>
  <action>
1. **Add MCP status section to SidebarWidget**:
   - Add `mcp_servers: []const MCPServerStatus` field to SidebarContext or widget data
   - MCPServerStatus struct: `{ name: []const u8, connected: bool, tool_count: u32 }`
   - Render section below existing sidebar sections (files, LSP diagnostics)
   - Header: "MCP Servers" with icon
   - Each server: `● server_name (N tools)` or `○ server_name (disconnected)`
   - Color: green dot for connected, red dot for disconnected, dim for unavailable

2. **Pass MCP status from Model to SidebarWidget**:
   - Add method to Model: `getMCPServerStatus()` that reads from mcp_bridge
   - During sidebar render, query current server status
   - Update status on server connect/disconnect events
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | head -30</automated>
  </verify>
  <done>
    - Sidebar shows MCP server list with connection status
    - Tool count per server visible
    - Connected/disconnected state clearly indicated
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP Server Auto-Discovery + Health Monitoring</name>
  <files>src/tui/chat_tui_app.zig</files>
  <action>
1. **Enhance MCP init with config file support**:
   - Read `~/.crushcode/mcp_servers.json` for user-configured servers
   - Merge with auto-discovered servers (config takes priority)
   - Log all discovered + configured servers at init

2. **Add periodic health check**:
   - Every 60 seconds, ping connected MCP servers (send `ping` method)
   - If server unresponsive: mark as disconnected in status, show toast
   - If server recovers: auto-reconnect, mark as connected, show toast

3. **Add `/mcp` slash command**:
   - `/mcp` — list all servers with status
   - `/mcp connect <name>` — connect to specific server
   - `/mcp disconnect <name>` — disconnect specific server
   - `/mcp discover` — re-run server discovery
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | head -30</automated>
  </verify>
  <done>
    - MCP servers auto-discovered from filesystem + config file
    - Health check runs periodically
    - Status updates reflected in sidebar
    - `/mcp` slash command for manual management
  </done>
</task>

</tasks>

<verification>
1. `zig build` compiles without errors
2. Sidebar shows MCP server section (even if empty: "No MCP servers")
3. If MCP server configured and running, shows as connected with tool count
4. `/mcp` command shows server status
</verification>

<success_criteria>
- MCP server status visible in TUI sidebar
- Auto-discovery + config file support for server setup
- Health monitoring keeps status current
- Users can manage servers via `/mcp` command
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-agent-loop/21-02-SUMMARY.md`
</output>
