---
phase: 66-reliability-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/streaming_parsers.zig
  - src/permission/evaluate.zig
  - src/guardrail/secrets.zig
  - src/guardrail/pii_scanner.zig
  - src/streaming/session.zig
  - src/usage/budget.zig
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03

must_haves:
  truths:
    - "streaming_parsers.zig has tests for SSE parsing, NDJSON parsing, finish_reason extraction"
    - "permission/evaluate.zig has tests for tool risk classification and permission decisions"
    - "guardrail/secrets.zig has tests for API key, token, password pattern detection"
    - "guardrail/pii_scanner.zig has tests for email, phone, SSN, credit card detection"
    - "streaming/session.zig has tests for session state transitions"
    - "usage/budget.zig has tests for budget checking and cost recording"
    - "All tests pass with zig build"
  artifacts:
    - path: "src/ai/streaming_parsers.zig"
      provides: "Streaming parser test coverage"
      contains: 'test "'
    - path: "src/permission/evaluate.zig"
      provides: "Permission evaluation test coverage"
      contains: 'test "'
    - path: "src/guardrail/secrets.zig"
      provides: "Secret detection test coverage"
      contains: 'test "'
    - path: "src/guardrail/pii_scanner.zig"
      provides: "PII scanner test coverage"
      contains: 'test "'
  key_links: []

---

<objective>
Add comprehensive test coverage to 6 security-critical and streaming-critical modules that currently have 0 tests. These modules guard against data leaks (secrets/PII to AI providers), handle streaming responses from AI providers, and enforce permission boundaries.

Total target: 60+ new tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Module contracts the executor needs. -->

### src/ai/streaming_parsers.zig (729L, 0 tests)
Key types:
- `StreamFormat` enum: sse, ndjson
- `StreamEvent` struct: type, content, tool_call data, finish_reason
- `parseSSELine()`, `parseNDJSONLine()` — line-by-line parsers
- `extractToolCalls()` — tool call extraction from accumulated content
- `detectFinishReason()` — finish_reason extraction

### src/permission/evaluate.zig (595L, 0 tests)
Key types:
- `PermissionMode` enum (imported from types.zig)
- `PermissionAction` enum: allow, deny, ask, auto_approve
- `evaluatePermission()` — main evaluation function
- `classifyToolRisk()` — risk tier classification
- `shouldAutoApprove()` — auto-approve decision

### src/guardrail/secrets.zig (282L, 0 tests)
Key types:
- `SecretPattern` struct: name, regex, description
- `detectSecrets()` — main detection function, returns list of findings
- `maskSecrets()` — replaces found secrets with [REDACTED]
- Built-in patterns for: AWS keys, GitHub tokens, API keys, private keys, passwords

### src/guardrail/pii_scanner.zig (369L, 0 tests)
Key types:
- `PIIType` enum: email, phone, ssn, credit_card, ip_address, etc.
- `PIIFinding` struct: type, value, position
- `detectPII()` — main detection, returns findings list
- `maskPII()` — replaces PII with [PII:TYPE]

### src/streaming/session.zig (213L, 0 tests)
Key types:
- `SessionState` enum: idle, streaming, tool_executing, waiting_input, error
- `StreamingSession` struct: state machine for session lifecycle
- `transition()` — state transition with validation

### src/usage/budget.zig (213L, 0 tests)
Key types:
- `BudgetManager` struct: tracks spending vs budget
- `checkBeforeRequest()` — returns error if over budget
- `recordCost()` — records token usage cost
- `isOverBudget()` — boolean check
- `getUtilization()` — percentage used
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to streaming_parsers.zig</name>
  <files>src/ai/streaming_parsers.zig</files>
  <read_first>
    - src/ai/streaming_parsers.zig (full file — understand all functions and types)
  </read_first>
  <action>
Read the full file first. Then add inline test blocks (`test "description" { ... }`) at the bottom of the file, BEFORE any closing `comptime` blocks.

Tests to add (target: 15+ tests):

For SSE parsing:
- `test "SSE parse data line"` — valid `data: {"content":"hello"}` → extracts content
- `test "SSE parse data line with tool_calls"` — `data: {"tool_calls":[...]}` → extracts tool calls
- `test "SSE parse finish_reason stop"` — `data: {"finish_reason":"stop"}` → detects stop
- `test "SSE parse finish_reason length"` — `data: {"finish_reason":"length"}` → detects length (critical for Phase 60)
- `test "SSE parse empty data line"` — `data: {}` → handles gracefully
- `test "SSE parse malformed JSON"` — `data: {broken` → returns error gracefully
- `test "SSE ignores non-data lines"` — lines like `: comment` or `event: ping` → skipped

For NDJSON parsing:
- `test "NDJSON parse valid line"` — `{"content":"hello","role":"assistant"}` → extracts content
- `test "NDJSON parse with tool_calls"` — NDJSON line with tool_calls → extracts correctly
- `test "NDJSON parse with finish_reason"` — detects finish_reason in NDJSON
- `test "NDJSON parse malformed"` — broken JSON → error handling

For finish_reason detection:
- `test "detectFinishReason stop"` — returns .stop
- `test "detectFinishReason length"` — returns .length
- `test "detectFinishReason null"` — no finish_reason → returns null
- `test "detectFinishReason tool_calls"` — returns .tool_calls

IMPORTANT:
- Read the ACTUAL function signatures before writing tests — don't guess parameter types
- Use `try testing.expect()` and `try testing.expectEqual()` from `std.testing`
- Handle allocator properly — use `std.testing.allocator` for tests
- Use `defer` for cleanup of allocated resources
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 15+ new test blocks in streaming_parsers.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `grep -c '^test "' src/ai/streaming_parsers.zig` returns 15+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add tests to permission/evaluate.zig + guardrail modules</name>
  <files>src/permission/evaluate.zig, src/guardrail/secrets.zig, src/guardrail/pii_scanner.zig</files>
  <read_first>
    - src/permission/evaluate.zig (full file)
    - src/guardrail/secrets.zig (full file)
    - src/guardrail/pii_scanner.zig (full file)
  </read_first>
  <action>
Read ALL THREE files completely before writing any tests. Understand the actual function signatures, parameter types, and return types.

**permission/evaluate.zig** — target 10+ tests:
- Test tool risk classification: read tools → safe, write tools → moderate, delete/shell → dangerous
- Test permission mode decisions: auto mode auto-approves safe tools, asks for moderate, denies dangerous
- Test shouldAutoApprove with various tool names
- Test boundary cases: unknown tool, empty tool name

**guardrail/secrets.zig** — target 10+ tests:
- Test AWS access key detection: `AKIAIOSFODNN7EXAMPLE` → found
- Test GitHub token detection: `ghp_xxxxxxxxxxxx` → found
- Test generic API key detection: various formats
- Test private key detection: `-----BEGIN RSA PRIVATE KEY-----` → found
- Test password in URL: `https://user:password@host` → found
- Test clean content: no secrets → returns empty list
- Test masking: verify secrets are replaced with `[REDACTED]`
- Test partial match: content with secret-like but not actual patterns

**guardrail/pii_scanner.zig** — target 10+ tests:
- Test email detection: `user@example.com` → found
- Test phone detection: various formats (US, international)
- Test SSN detection: `123-45-6789` → found
- Test credit card detection: `4111111111111111` → found
- Test IP address detection: `192.168.1.1` → found
- Test clean content: no PII → empty list
- Test masking: verify PII replaced with `[PII:EMAIL]` etc.
- Test multiple PII types in same content

IMPORTANT:
- Read ACTUAL function signatures — these modules may use different patterns than expected
- Use `std.testing.allocator` for all allocations in tests
- Use `defer` for cleanup
- For regex-heavy modules, test both positive and negative cases
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 10+ tests in permission/evaluate.zig
    - 10+ tests in guardrail/secrets.zig
    - 10+ tests in guardrail/pii_scanner.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `grep -c '^test "' src/permission/evaluate.zig` returns 10+
    - `grep -c '^test "' src/guardrail/secrets.zig` returns 10+
    - `grep -c '^test "' src/guardrail/pii_scanner.zig` returns 10+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tests to streaming/session.zig + usage/budget.zig</name>
  <files>src/streaming/session.zig, src/usage/budget.zig</files>
  <read_first>
    - src/streaming/session.zig (full file)
    - src/usage/budget.zig (full file)
  </read_first>
  <action>
Read BOTH files completely before writing tests.

**streaming/session.zig** — target 8+ tests:
- Test session state transitions: idle → streaming → idle (happy path)
- Test streaming → tool_executing → streaming (tool call cycle)
- Test invalid transitions: idle → tool_executing (should fail/return error)
- Test error state handling
- Test session reset
- Test state name string representation

**usage/budget.zig** — target 8+ tests:
- Test BudgetManager init with specific budget amount
- Test recordCost: add cost, verify total increases
- Test checkBeforeRequest: under budget → returns ok
- Test checkBeforeRequest: over budget → returns error
- Test isOverBudget: at 0% → false, at 100% → true
- Test getUtilization: returns correct percentage
- Test edge: zero budget, negative cost, exact budget match
- Test warning threshold (80%)

IMPORTANT:
- Read actual struct fields and method signatures
- BudgetManager likely takes allocator as first param
- Use std.testing.allocator
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 8+ tests in streaming/session.zig
    - 8+ tests in usage/budget.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `grep -c '^test "' src/streaming/session.zig` returns 8+
    - `grep -c '^test "' src/usage/budget.zig` returns 8+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests read production types but don't modify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | I | test data | accept | Test data uses fake secrets/PII — no real credentials |
</threat_model>

<verification>
1. `zig build` succeeds with no errors
2. `grep -c '^test "' src/ai/streaming_parsers.zig` returns 15+
3. `grep -c '^test "' src/permission/evaluate.zig` returns 10+
4. `grep -c '^test "' src/guardrail/secrets.zig` returns 10+
5. `grep -c '^test "' src/guardrail/pii_scanner.zig` returns 10+
6. `grep -c '^test "' src/streaming/session.zig` returns 8+
7. `grep -c '^test "' src/usage/budget.zig` returns 8+
8. Total: 61+ new tests across 6 files
</verification>

<success_criteria>
- 60+ new tests across 6 security-critical and streaming-critical modules
- All tests pass
- Build clean
- No production code changes (tests only)
</success_criteria>

<output>
After completion, create `.planning/phases/66-reliability-tests/66-01-SUMMARY.md`
</output>
