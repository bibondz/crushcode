---
phase: 01-core-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/client.zig
  - src/commands/chat.zig
  - src/cli/args.zig
  - src/config/config.zig
autonomous: true
requirements:
  - AI-04
  - BD-01
  - BD-02
  - BD-03

must_haves:
  truths:
    - "User can specify a system prompt that sets AI context"
    - "User can chat with AI via CLI receives streaming responses"
    - "User can switch between AI providers during chat"
    - "`zig build` produces working crushcode executable with no errors"
    - "Help text displays for all commands with user-friendly error messages"
  artifacts:
    - path: "src/ai/client.zig"
      provides: "AI client with streaming and system prompt support"
      min_lines: 50
    - path: "src/commands/chat.zig"
      provides: "Chat command with streaming output"
      min_lines: 20
    - path: "zig-out/bin/crushcode"
      provides: "Working executable"
      min_size: 9000000
  key_links:
    - from: "src/commands/chat.zig"
      to: "src/ai/client.zig"
      via: "client_mod.sendChat()"
      pattern: "sendChat"
    - from: "src/config/config.zig"
      to: "src/ai/client.zig"
      via: "api_key from config"
      pattern: "getApiKey"
---

<objective>
Implement system prompt support and enable streaming AI responses.

Purpose: Allow users to set AI context via system prompts and see responses as they stream in (better UX for long responses).

Output: Working chat with system prompts and streaming responses.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@src/ai/client.zig
@src/commands/chat.zig
@src/config/config.zig
@build.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add system prompt support</name>
  <files>src/ai/client.zig, src/commands/chat.zig, src/config/config.zig</files>
  <action>
    **Modify src/config/config.zig:**
    - Add `system_prompt` field to Config struct
    - Parse `system_prompt` from TOML config file in parseKeyValue()
    - Add getter: `pub fn getSystemPrompt(self: *Config) ?[]const u8`

    **Modify src/ai/client.zig:**
    - Add optional `system_prompt: ?[]const u8` parameter to `sendChatWithHistory()`
    - When sending messages, prepend a system message as the first message if system_prompt is set
    - Build messages array with system message first, then user history

    **Modify src/commands/chat.zig:**
    - Pass system prompt from config to AI client
    - For interactive mode: Include system prompt in first message batch

    Reference existing code patterns:
    - config.getApiKey() for getter pattern
    - Message structure from client.zig: `ChatMessage{ .role = "system", .content = system_prompt }`
  </action>
  <verify>
    <automated>zig build 2>&1</automated>
  </verify>
  <done>User can set system_prompt in config.toml and it affects AI responses</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement streaming response support</name>
  <files>src/ai/client.zig</files>
  <action>
    **Modify src/ai/client.zig:**
    - Add streaming output support to sendChat()
    - For interactive mode (--streaming flag), print response chunks as they arrive
    - Use chunked output: print content immediately when received instead of waiting for full response

    Implementation approach:
    - Check if provider supports streaming (ollama does by default with SSE)
    - Add print statements for each content chunk
    - Gracefully handle non-streaming providers (current behavior as fallback)

    Reference existing flow in client.zig:
    - Ollama already does line-by-line parsing for streaming
    - Reuse that pattern for other providers when streaming enabled
  </action>
  <verify>
    <automated>zig build 2>&1 && ./zig-out/bin/crushcode --help | head -20</automated>
  </verify>
  <done>User sees response progressively when using --streaming flag</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and documentation</name>
  <files>build.zig</files>
  <action>
    **Run build verification:**
    - `zig build` must complete without errors
    - Executable at `zig-out/bin/crushcode` should be ~9MB+

    **Final verification commands:**
    - `./zig-out/bin/crushcode --help` shows help
    - `./zig-out/bin/crushcode version` shows version
    - `./zig-out/bin/crushcode list` lists providers
  </action>
  <verify>
    <automated>zig build 2>&1 && ls -la zig-out/bin/crushcode</automated>
  </verify>
  <done>Build produces working executable, all commands functional</done>
</task>

</tasks>

<verification>
1. Run `zig build` — no compilation errors
2. Run `./zig-out/bin/crushcode --help` — displays help
3. Run `./zig-out/bin/crushcode version` — shows version
4. Run `./zig-out/bin/crushcode list` — lists providers
5. Test with actual AI provider (if API key available):
   - `crushcode chat "Hello" --provider openai` — gets response
</verification>

<success_criteria>
- [ ] `zig build` produces crushcode executable
- [ ] All 16 Phase 1 requirements satisfied (AI-01 through BD-03)
- [ ] Help text displays for all commands
- [ ] Error messages are user-friendly
- [ ] Executable runs all basic commands (chat, read, list, help, version)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>