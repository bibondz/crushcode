---
phase: 02-shell-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/pty_cmd.zig
  - src/commands/handlers.zig
autonomous: true
requirements:
  - SH-03

must_haves:
  truths:
    - "User can run interactive commands via PTY sessions"
    - "User can list, kill, and interact with PTY sessions"
    - "Commands that require a terminal (vim, htop) work correctly"
  artifacts:
    - path: "src/commands/pty_cmd.zig"
      provides: "PTY command handler for CLI"
      min_lines: 60
    - path: "src/plugins/pty.zig"
      provides: "PTY session management (already exists)"
      existing: true
  key_links:
    - from: "src/commands/pty_cmd.zig"
      to: "src/plugins/pty.zig"
      via: "import and call"
      pattern: "PTYPlugin"
    - from: "src/main.zig"
      to: "src/commands/pty_cmd.zig"
      via: "dispatch"
      pattern: "pty.*handlePty"
---

<objective>
Integrate PTY plugin with CLI for interactive command support

**Purpose:** Enable interactive commands that require a terminal (vim, htop, etc.) through PTY sessions.

**Output:** Working `crushcode pty` subcommand with spawn, list, kill capabilities
</objective>

<execution_context>
@get-shit-done/workflows/execute-plan.md
@get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shell-execution/02-RESEARCH.md
@src/plugins/pty.zig
@src/commands/handlers.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PTY subcommand to args</name>
  <files>src/cli/args.zig</files>
  <behavior>
    - Parses "pty" as a subcommand
    - Supports "pty spawn <command>", "pty list", "pty kill <session>"
    - Captures arguments for each action
  </behavior>
  <read_first>
    @src/cli/args.zig
  </read_first>
  <action>
    Extend Args struct to handle PTY subcommand:
    - Add pty field with action type (spawn, list, kill, read)
    - Parse "pty" as command with sub-action
    - For spawn: capture command parts after "pty spawn"
    - For kill: capture session_id
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "error" | head -10 || echo "Build OK"</automated>
  </verify>
  <done>Args.parse recognizes pty subcommand and captures action/args</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PTY command handler</name>
  <files>src/commands/pty_cmd.zig</files>
  <behavior>
    - Spawn creates new PTY session and returns session ID
    - List shows all active sessions
    - Kill terminates a session
    - Read gets output from session
  </behavior>
  <read_first>
    @src/plugins/pty.zig
    @src/commands/chat.zig
  </read_first>
  <action>
    Create src/commands/pty_cmd.zig:
    - Import PTYPlugin from plugins/pty.zig
    - Implement handlePty(args: Args) !void
    - Handle sub-actions: spawn, list, kill, read
    - For spawn: call PTYPlugin.spawnTerminal() with command
    - For list: call PTYPlugin.listSessions()
    - For kill: call PTYPlugin.killSession()
    - Print results in user-friendly format
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "error" | head -10 || echo "Build OK"</automated>
  </verify>
  <done>PTY handler implements all PTY operations</done>
</task>

<task type="auto">
  <name>Task 3: Wire PTY command in handlers</name>
  <files>src/commands/handlers.zig, src/main.zig</files>
  <read_first>
    @src/main.zig
    @src/commands/handlers.zig
  </read_first>
  <action>
    Add pty command to dispatch:
    - Update isCommandRecognized() to include "pty"
    - Import pty_cmd module
    - Add pty case to command dispatch
    - Call handlePty(parsed_args)
  </action>
  <verify>
    <automated>./zig-out/bin/crushcode pty list 2>&1</automated>
  </verify>
  <done>crushcode pty command is functional</done>
</task>

</tasks>

<verification>
- `crushcode pty spawn ls -la` creates session and returns session ID
- `crushcode pty list` shows active sessions
- `crushcode pty kill <session_id>` terminates session
- Interactive commands work via PTY
</verification>

<success_criteria>
PTY integration enables interactive terminal commands
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-execution/02-02-SUMMARY.md`
</output>