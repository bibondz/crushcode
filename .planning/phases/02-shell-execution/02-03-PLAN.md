---
phase: 02-shell-execution
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/commands/shell.zig
  - src/config/config.zig
autonomous: true
requirements:
  - SH-04

must_haves:
  truths:
    - "User can specify timeout for shell commands"
    - "Long-running commands are terminated after timeout"
    - "Timeout is configurable via CLI flag or config file"
  artifacts:
    - path: "src/commands/shell.zig"
      provides: "Timeout support for shell execution"
      min_lines: 120
    - path: "src/config/config.zig"
      provides: "Timeout configuration option"
      exports: ["timeout_seconds"]
  key_links:
    - from: "src/commands/shell.zig"
      to: "std.time.Timer"
      via: "timeout implementation"
      pattern: "Timer.*start"
---

<objective>
Add configurable command timeout support

**Purpose:** Allow users to set a maximum execution time for shell commands, with automatic termination on timeout.

**Output:** `--timeout` flag on shell command and config option for default timeout
</objective>

<execution_context>
@get-shit-done/workflows/execute-plan.md
@get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shell-execution/02-RESEARCH.md
@src/commands/shell.zig
@src/config/config.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timeout config option</name>
  <files>src/config/config.zig</files>
  <behavior>
    - Config struct includes timeout_seconds field
    - Config loads timeout from TOML if present
    - Default timeout is 300 seconds (5 minutes)
  </behavior>
  <read_first>
    @src/config/config.zig
  </read_first>
  <action>
    Extend Config struct:
    - Add `timeout_seconds: u32 = 300` field
    - Add to TOML parsing in loadOrCreateConfig
    - Add to saveConfig if needed
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "error" | head -10 || echo "Build OK"</automated>
  </verify>
  <done>Config has timeout_seconds with default 300</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timeout flag to args</name>
  <files>src/cli/args.zig</files>
  <behavior>
    - Parses --timeout <seconds> flag
    - Accepts numeric value for timeout
  </behavior>
  <read_first>
    @src/cli/args.zig
  </read_first>
  <action>
    Extend Args struct:
    - Add `timeout: ?u32 = null` field
    - Parse --timeout flag in parse() function
    - Store parsed value in Args
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "error" | head -10 || echo "Build OK"</automated>
  </verify>
  <done>Args.parse handles --timeout flag</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement timeout in shell handler</name>
  <files>src/commands/shell.zig</files>
  <behavior>
    - Command execution respects timeout
    - If timeout exceeded, process is killed
    - User sees "Command timed out after X seconds" message
  </behavior>
  <read_first>
    @src/commands/shell.zig
    @src/commands/pty.zig
  </read_first>
  <action>
    Implement timeout logic in shell command:
    - Use std.time.Timer to track elapsed time
    - After spawning, check timer in loop while waiting
    - If timeout reached, kill process with SIGKILL
    - Print timeout message to stderr
    - Handle both simple execution and PTY cases
    - Use config timeout as default, CLI --timeout overrides
  </action>
  <verify>
    <automated>./zig-out/bin/crushcode shell --timeout 1 sleep 10 2>&1</automated>
  </verify>
  <done>Shell commands respect timeout and terminate on expiry</done>
</task>

</tasks>

<verification>
- `crushcode shell --timeout 5 sleep 10` times out after 5 seconds
- Default timeout used when --timeout not specified
- Timeout message displayed on timeout
- Process properly terminated (no zombie)
</verification>

<success_criteria>
Shell commands can be terminated after configurable timeout
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-execution/02-03-SUMMARY.md`
</output>