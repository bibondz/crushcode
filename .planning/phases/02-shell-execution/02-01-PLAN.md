---
phase: 02-shell-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/handlers.zig
  - src/commands/shell.zig
  - src/cli/args.zig
autonomous: true
requirements:
  - SH-01
  - SH-02

must_haves:
  truths:
    - "User can execute shell commands via 'crushcode shell <command>' and see output"
    - "Command execution returns exit code visible to user"
    - "stderr and stdout are captured and displayed"
  artifacts:
    - path: "src/commands/shell.zig"
      provides: "Shell command handler implementation"
      min_lines: 80
    - path: "src/cli/args.zig"
      provides: "Shell command argument parsing"
      exports: ["shell"]
    - path: "src/commands/handlers.zig"
      provides: "Shell command dispatch"
      exports: ["handleShell"]
  key_links:
    - from: "src/main.zig"
      to: "src/commands/handlers.zig"
      via: "command dispatch"
      pattern: "shell.*handleShell"
    - from: "src/commands/shell.zig"
      to: "std.process.Child"
      via: "command execution"
      pattern: "Child\\.init.*spawn"
---

<objective>
Add shell command to CLI that executes commands and returns output with exit codes

**Purpose:** Enable users to execute shell commands from the CLI and see results. This is the foundation for AI-driven shell interactions.

**Output:** Working `crushcode shell <command>` that captures output and exit codes.
</objective>

<execution_context>
@get-shit-done/workflows/execute-plan.md
@get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-shell-execution/02-RESEARCH.md
@src/main.zig
@src/commands/handlers.zig
@src/cli/args.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shell command argument parsing</name>
  <files>src/cli/args.zig</files>
  <behavior>
    - Parses "shell" as a command
    - Captures remaining arguments as shell command parts
    - Returns Args struct with shell command info
  </behavior>
  <read_first>
    @src/cli/args.zig
  </read_first>
  <action>
    Add shell command parsing to Args struct and parse() function:
    - Add `shell: ?struct { command: []const u8, args: [][]const u8 }` field to Args
    - In parse(), detect "shell" command and capture remaining args as command parts
    - Return error.UnknownCommand if shell command given without arguments
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "(error|warning)" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>Args.parse recognizes "shell" command and captures command parts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shell command handler</name>
  <files>src/commands/shell.zig</files>
  <behavior>
    - Executes provided command using std.process.Child
    - Captures stdout and stderr
    - Returns exit code to user
    - Handles command not found, permission errors
  </behavior>
  <read_first>
    @src/commands/chat.zig
    @src/commands/handlers.zig
  </read_first>
  <action>
    Create src/commands/shell.zig with:
    - `handleShell(args: Args) !void` function
    - Use `std.process.Child.init()` with command_parts
    - Set stdout/stderr behavior to .Capture
    - Spawn process, wait for completion
    - Print stdout/stderr to stdout
    - Print exit code after execution
    - Handle errors: command not found, exec failed, wait failed
    - Follow patterns from chat.zig for error handling
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "(error|warning)" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>Shell handler executes commands and returns output + exit code</done>
</task>

<task type="auto">
  <name>Task 3: Wire shell command in handlers</name>
  <files>src/commands/handlers.zig</files>
  <read_first>
    @src/commands/handlers.zig
  </read_first>
  <action>
    Update isCommandRecognized() to include "shell"
    Update main dispatch in main.zig to handle shell command:
    - Add import for shell module
    - Add condition for shell command
    - Call handleShell(parsed_args)
  </action>
  <verify>
    <automated>./zig-out/bin/crushcode shell echo hello 2>&1</automated>
  </verify>
  <done>crushcode shell command is functional</done>
</task>

</tasks>

<verification>
- `zig build` produces executable with no errors
- `crushcode shell echo hello` outputs "hello" and exit code 0
- `crushcode shell false` returns exit code 1
- `crushcode shell nonexistentcmd` shows error message
</verification>

<success_criteria>
User can execute shell commands via CLI and receive output + exit code
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-execution/02-01-SUMMARY.md`
</output>