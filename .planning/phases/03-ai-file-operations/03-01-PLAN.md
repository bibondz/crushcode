---
phase: 03-ai-file-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/write.zig
  - src/cli/args.zig
  - src/commands/handlers.zig
autonomous: true
requirements:
  - FO-02
  - FO-03

must_haves:
  truths:
    - "AI agent can write files based on user requests (e.g., 'write this config file')"
    - "AI agent can edit files based on user requests (e.g., 'add error handling to main.zig')"
    - "Glob patterns work for multi-file operations (*.txt, **/*.zig)"
    - "File operations succeed or return clear error messages"
  artifacts:
    - path: "src/commands/write.zig"
      provides: "File write/edit operations with glob support"
      min_lines: 150
      exports: ["writeFile", "writeFiles", "handleWrite", "handleEdit", "globMatch"]
    - path: "src/commands/chat.zig"
      provides: "AI-driven file operation integration"
      contains: "file.*write|write.*file|edit.*file"
    - path: "src/cli/args.zig"
      provides: "Write command argument parsing"
      exports: ["write"]
  key_links:
    - from: "src/commands/chat.zig"
      to: "src/commands/write.zig"
      via: "AI invokes write operations"
      pattern: "call.*write|invoke.*write"
    - from: "src/commands/write.zig"
      to: "std.fs"
      via: "file system operations"
      pattern: "createFile|openFile"
---

<objective>
Complete AI-driven file operations - enable AI agent to write and edit files based on user requests, with glob pattern support

**Purpose:** Users can ask AI to "write this file" or "edit all *.test.ts files" and AI will perform these operations. This builds on Phase 1 (AI chat) and Phase 2 (shell execution).

**Output:** Working AI file operations with glob support.
</objective>

<execution_context>
@get-shit-done/workflows/execute-plan.md
@get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/commands/write.zig
@src/commands/handlers.zig
@src/commands/chat.zig
@src/cli/args.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement glob pattern matching in write.zig</name>
  <files>src/commands/write.zig</files>
  <behavior>
    - globMatch() takes a pattern and returns matching file paths
    - Supports wildcards: *, ?, ** (recursive)
    - Returns error on invalid pattern or no matches
    - Works with Zig stdlib only (no external deps)
  </behavior>
  <read_first>
    @src/commands/write.zig
  </read_first>
  <action>
    Add globMatch function to src/commands/write.zig:
    - `pub fn globMatch(pattern: []const u8) ![][]const u8`
    - Use `std.fs.walk` for recursive matching
    - Support * (any chars), ? (single char), **/* (recursive)
    - Return ArrayList of matching paths
    - Handle errors: invalid pattern, permission denied, no matches
    
    Replace the TODO in writeFiles() to use globMatch for glob patterns.
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "(error|warning)" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>Glob patterns (*.txt, **/*.zig) match files correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add write command to CLI args</name>
  <files>src/cli/args.zig</files>
  <read_first>
    @src/cli/args.zig
  </read_first>
  <action>
    Add "write" to command detection in args.zig:
    - Add `write: ?struct { path: []const u8, content: []const u8 }` to Args
    - Parse "write" command similar to "read" and "shell"
    - Capture --content or remaining args as content
    - Add error.UnknownCommand if write without path
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "(error|warning)" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>Args.parse recognizes "write" command with path and content</done>
</task>

<task type="auto">
  <name>Task 3: Wire write command in handlers</name>
  <files>src/commands/handlers.zig</files>
  <read_first>
    @src/commands/handlers.zig
  </read_first>
  <action>
    Ensure handleWrite is called for write command:
    - Already imported in handlers.zig (line 8)
    - verify isCommandRecognized includes "write"
    - verify dispatch handles write command
  </action>
  <verify>
    <automated>./zig-out/bin/crushcode write test.txt "hello" 2>&1</automated>
  </verify>
  <done>crushcode write command creates files</done>
</task>

<task type="auto">
  <name>Task 4: Add AI-driven file operation prompts</name>
  <files>src/commands/chat.zig</files>
  <read_first>
    @src/commands/chat.zig
  </read_first>
  <action>
    Enhance chat to detect file operation requests:
    - Add detection for phrases like "write file", "create file", "edit file"
    - Extract file path and content from user message
    - After AI response, offer to perform file operations
    
    Pattern: User says "write config.toml with X" → AI generates content → prompt user to confirm → write_mod handles actual write
    
    Alternatively, detect natural language file ops:
    - "write  with <content>" → extract and execute
    - "edit  to add <code>" → extract and execute
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -E "(error|warning)" | head -20 || echo "Build OK"</automated>
  </verify>
  <done>AI can suggest/write files based on user requests</done>
</task>

</tasks>

<verification>
- `zig build` produces executable with no errors
- `crushcode write test.txt "hello"` creates test.txt with "hello"
- Glob pattern matches work: `crushcode write "*.txt" --content "test"` 
- AI chat can suggest file operations
</verification>

<success_criteria>
User can ask AI to "write this file" and AI creates/modifies the file
User can specify file targets using glob patterns (e.g., "edit all *.test.ts files")
File operations succeed or return clear error messages
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-file-operations/03-01-SUMMARY.md`
</output>