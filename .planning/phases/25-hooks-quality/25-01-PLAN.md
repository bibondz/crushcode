# Plan 25-01: Wire Lifecycle Hooks + Honest Rename + Code Cleanup

**Phase:** 25 (Lifecycle Hooks + Code Quality)
**Goal:** Wire LifecycleHooks into TUI agent loop, register core hooks, rename ast_grep → pattern_search, clean up dead code

## Tasks

### Task 1: Add LifecycleHooks to TUI Model
- Add `const lifecycle_mod = @import("lifecycle");` import at top of `chat_tui_app.zig`
- Add field: `lifecycle_hooks: lifecycle_mod.LifecycleHooks` to Model struct (after `mcp_bridge` field around line 254)
- Init in `create()`: `.lifecycle_hooks = lifecycle_mod.LifecycleHooks.init(allocator)`
- Deinit in `destroy()`: `self.lifecycle_hooks.deinit()`
- Ensure `lifecycle_mod` is available in build.zig (check if already imported; add if not)

### Task 2: Register Core Hooks
- In `create()`, after init of `lifecycle_hooks`, register these core hooks:
  1. `"token_tracker"` — `HookPhase.post_request`, core tier, priority 10 — logs token counts
  2. `"error_logger"` — `HookPhase.on_error`, core tier, priority 10 — logs errors
  3. `"tool_timer"` — `HookPhase.post_tool`, core tier, priority 10 — logs tool execution timing
- Hook handler functions: standalone `fn` in chat_tui_app.zig that log via `std.log.info`
- These are simple logging hooks — no complex state

### Task 3: Add Hook Call Sites in runStreamingRequest
- **pre_request**: Before `sendChatStreamingWithFallback()` call (around line 2556), create a HookContext and execute `.pre_request` phase
- **post_request**: After `applyAssistantResponse()` (around line 2572), execute `.post_request` with token counts
- **on_error**: In error paths (where `finishRequestWithErrorText` is called), execute `.on_error`

### Task 4: Add Hook Call Sites in executeToolCalls
- **pre_tool**: Before `requestToolPermission()` call (around line 2676), execute `.pre_tool` with tool_name
- **post_tool**: After tool execution completes (after result_text is obtained), execute `.post_tool`

### Task 5: Session Start/End Hooks
- **session_start**: In `initConversation()` or `Model.create()` after init, execute `.session_start`
- **session_end**: In `destroy()` before deinit, execute `.session_end`

### Task 6: Rename ast_grep.zig → pattern_search.zig
- Rename file: `src/edit/ast_grep.zig` → `src/edit/pattern_search.zig`
- Update `build.zig` line 326: change path and variable name
- Update `build.zig` line 418: update import name reference
- Update `build.zig` line 483: update module reference
- Update `src/commands/handlers/tools.zig`: change `@import("ast_grep")` → `@import("pattern_search")` and variable names `ast_grep_mod` → `pattern_search_mod`
- Update `STRUCTURE.md` line 69: update description

### Task 7: Clean Up Dead Code in graph/types.zig
- Add comment to unused EdgeType variants (`inherits`, `implements`, `depends_on`, `tests`) noting they're reserved for future use
- Remove Community type if it's genuinely unused in production (verify first)
- Keep the enum variants — they're intentionally forward-looking for the knowledge graph

## Verification
- `zig build` passes clean
- `zig build test` passes
- TUI starts without crash
- Hooks print to log on events

## Constraints
- Zig 0.15.2 API: `ArrayList.append(allocator, item)`, `splitScalar()` returns iterator directly
- Always: `export CI=true GIT_TERMINAL_PROMPT=0 GIT_EDITOR=: GIT_PAGER=at`
- Integration-first: wire before build check
