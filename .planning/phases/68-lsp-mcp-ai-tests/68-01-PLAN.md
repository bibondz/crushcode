---
phase: 68-lsp-mcp-ai-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/error_handler.zig
  - src/ai/fallback.zig
  - src/cli/intent_gate.zig
  - src/lsp/client.zig
  - src/mcp/discovery.zig
  - src/search/semantic.zig
autonomous: true
requirements:
  - REL-05

must_haves:
  truths:
    - "error_handler.zig has tests for HTTP status parsing, retry delay, error classification"
    - "fallback.zig has tests for chain management, next-fallback selection, primary/isEmpty"
    - "intent_gate.zig has tests for intent classification across all 7 intent types"
    - "lsp/client.zig has tests for request formatting, response parsing"
    - "mcp/discovery.zig has tests for server config parsing, capability matching"
    - "search/semantic.zig has tests for cosine similarity, embedding operations"
    - "All tests pass with zig build"
  artifacts:
    - path: "src/ai/error_handler.zig"
      provides: "Error handler test coverage"
      contains: 'test "'
    - path: "src/ai/fallback.zig"
      provides: "Fallback chain test coverage"
      contains: 'test "'
    - path: "src/cli/intent_gate.zig"
      provides: "Intent gate test coverage"
      contains: 'test "'
    - path: "src/lsp/client.zig"
      provides: "LSP client test coverage"
      contains: 'test "'
    - path: "src/mcp/discovery.zig"
      provides: "MCP discovery test coverage"
      contains: 'test "'
    - path: "src/search/semantic.zig"
      provides: "Semantic search test coverage"
      contains: 'test "'
  key_links: []

---

<objective>
Add test coverage to 6 AI/infra layer files (all currently 0 tests). Focus on pure-logic functions: error classification, chain management, intent scoring, JSON parsing, and math operations.

Target: 50+ new tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- AI/infra layer contracts the executor needs -->

### src/ai/error_handler.zig (189L, 0 tests)
Key pure functions:
- `parseHttpStatus(status: u16, body: []const u8) ?ErrorResponse` — maps HTTP codes to error types
- `calculateDelay(attempt: u32, config: RetryConfig) u32` — exponential backoff calculation
- `isRetryableError(error_type: AIClientError) bool` — retry decision
- `formatError(err: ErrorResponse) []const u8` — human-readable error messages
- `RetryConfig.default()`, `.aggressive()`, `.conservative()` — preset configs
- `RateLimiter.init/checkRateLimit/deinit` — rate limiting
- `ErrorResponse.init/withCode/withRetryAfter` — constructors

### src/ai/fallback.zig (133L, 0 tests)
Key functions:
- `FallbackChain.init(allocator) FallbackChain` — creates chain
- `FallbackChain.addEntry(provider, model) !void` — appends entry
- `FallbackChain.getPrimary() ?FallbackEntry` — returns first entry
- `FallbackChain.getNext(provider, model) ?FallbackEntry` — returns next after current
- `FallbackChain.getAtIndex(index) ?FallbackEntry` — index access
- `FallbackChain.isEmpty() bool` — empty check
- `FallbackChain.count() usize` — entry count
- `FallbackChain.setRetryDelay(ms)` / `setMaxRetries(n)` — configuration

### src/cli/intent_gate.zig (193L, 0 tests)
Key functions:
- `IntentGate.init(allocator) IntentGate` — creates gate with keyword tables
- `IntentGate.classify(message: []const u8) IntentResult` — classifies intent
- `IntentGate.intentLabel(intent: IntentType) []const u8` — human label
- `IntentType` enum: research, implementation, investigation, evaluation, fix, open_ended, chat
- `IntentResult` struct: intent_type, confidence (f64), keywords_matched, suggested_action

### src/lsp/client.zig (639L, 0 tests)
Key functions to test (pure-logic extraction):
- Request ID generation/formatting
- JSON-RPC request construction
- LSP response parsing (diagnostics, completion items, hover info)
- Position encoding/decoding (line:character)

### src/mcp/discovery.zig (475L, 0 tests)
Key functions to test:
- `MCPDiscovery` — server config parsing
- `MCPDiscoveryResult` — result structure
- `MCPServerType` enum: stdio, sse, streamable_http
- Config validation logic
- Server capability matching

### src/search/semantic.zig (372L, 0 tests)
Key functions:
- `cosineSimilarity(a: []const f32, b: *const [embed_dim]f32) f64` — vector similarity
- `SemanticIndex` struct — embedding storage
- `SearchResult` struct — ranked results
- `EmbeddingEntry` struct — stored embeddings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to error_handler.zig, fallback.zig, intent_gate.zig</name>
  <files>src/ai/error_handler.zig, src/ai/fallback.zig, src/cli/intent_gate.zig</files>
  <read_first>
    - src/ai/error_handler.zig (full file — 189L)
    - src/ai/fallback.zig (full file — 133L)
    - src/cli/intent_gate.zig (full file — 193L)
  </read_first>
  <action>
Read ALL THREE files completely before writing tests. All functions and types are documented above in the interfaces section.

**error_handler.zig** (0 tests → target 12+ tests):

For `parseHttpStatus`:
- test parseHttpStatus 400 → InvalidRequest
- test parseHttpStatus 401 → AuthenticationError
- test parseHttpStatus 403 → AuthenticationError (forbidden)
- test parseHttpStatus 404 → ModelNotFound
- test parseHttpStatus 429 → RateLimitError with retry_after=60
- test parseHttpStatus 500 → ServerError
- test parseHttpStatus 200 → null (success)
- test parseHttpStatus 999 → null (unknown)

For `calculateDelay`:
- test calculateDelay attempt 0 → base_delay
- test calculateDelay attempt 2 → base * multiplier^2
- test calculateDelay respects max_delay_ms cap
- test RetryConfig.default/.aggressive/.conservative preset values

For `isRetryableError`:
- test NetworkError/RateLimitError/ServerError/TimeoutError → true
- test AuthenticationError/TokenLimitError → false

For `formatError`:
- test each error type returns non-empty string

**fallback.zig** (0 tests → target 10+ tests):
- test init creates empty chain, isEmpty returns true
- test addEntry + count = 1, isEmpty = false
- test addEntry 3 providers + getPrimary returns first
- test getNext from first returns second
- test getNext from last returns null
- test getNext for unknown provider returns first
- test getAtIndex valid/invalid indices
- test setRetryDelay / setMaxRetries update values
- test deinit frees all allocated strings (use std.testing.allocator to detect leaks)
- test getEntries returns correct slice

**intent_gate.zig** (0 tests → target 12+ tests):
- test classify "implement X" → .implementation with confidence > 0
- test classify "explain Y" → .research
- test classify "fix the bug" → .fix
- test classify "what do you think" → .evaluation
- test classify "check the logs" → .investigation
- test classify "refactor the module" → .open_ended
- test classify "hello" → .chat (no keyword match)
- test classify "implement X and explain Y" → highest scoring intent wins
- test classify empty string → .chat with low confidence
- test intentLabel returns non-empty for all IntentType values
- test confidence is clamped to [0.0, 1.0]
- test classify is case-insensitive ("IMPLEMENT" same as "implement")

IMPORTANT:
- Use `std.testing.allocator` for all tests
- Use `defer` for cleanup of FallbackChain, IntentGate
- For intent_gate tests: the classify() method allocates lowercase copy — defer free
- fallback.zig addEntry dupes strings — must deinit to avoid leak detection
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 12+ tests in error_handler.zig
    - 10+ tests in fallback.zig
    - 12+ tests in intent_gate.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/ai/error_handler.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 12+
    - `Select-String -Path src/ai/fallback.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 10+
    - `Select-String -Path src/cli/intent_gate.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 12+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add tests to lsp/client.zig, mcp/discovery.zig, search/semantic.zig</name>
  <files>src/lsp/client.zig, src/mcp/discovery.zig, src/search/semantic.zig</files>
  <read_first>
    - src/lsp/client.zig (full file — 639L)
    - src/mcp/discovery.zig (full file — 475L)
    - src/search/semantic.zig (full file — 372L)
  </read_first>
  <action>
Read ALL THREE files completely before writing tests. These are larger files — focus on the pure-logic functions listed in the interfaces section.

**lsp/client.zig** (639L, 0 tests → target 8+ tests):
Find and test these pure-logic patterns:
- JSON-RPC request construction: verify correct method, params structure
- Response parsing: extract diagnostics from JSON (ParseError → DiagnosticItem)
- Completion item parsing: text, sortText, detail extraction
- Position encoding: line/character to LSP Position struct
- Document URI formatting: file:// prefix, path encoding
- Error response handling
- If the file is mostly I/O (socket connections), extract testable helpers into test blocks

**mcp/discovery.zig** (475L, 0 tests → target 8+ tests):
- test MCPServerType enum values: stdio, sse, streamable_http
- test MCPDiscoveryResult construction
- test server config parsing: valid JSON → correct struct
- test server config parsing: invalid JSON → error
- test capability matching: tool name vs server capabilities
- test server type detection from URL patterns
- test empty config handling
- test multiple servers in config

**search/semantic.zig** (372L, 0 tests → target 10+ tests):
- test cosineSimilarity identical vectors → 1.0 (or very close)
- test cosineSimilarity orthogonal vectors → 0.0
- test cosineSimilarity opposite vectors → -1.0
- test cosineSimilarity different lengths handled correctly
- test EmbeddingEntry construction and field access
- test SearchResult sorting: higher similarity first
- test SemanticIndex init/deinit lifecycle
- test empty index returns no results
- test index with entries returns ranked results
- test cosineSimilarity with zero vector → 0.0 (avoid division by zero)

IMPORTANT:
- lsp/client.zig and mcp/discovery.zig may require allocator for JSON parsing
- Use `std.testing.allocator` — will detect leaks automatically
- For semantic.zig: cosineSimilarity takes `[]const f32` and `*const [embed_dim]f32` — check actual embed_dim value
- For LSP: look for struct constructors and parsing functions, not socket operations
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 8+ tests in lsp/client.zig
    - 8+ tests in mcp/discovery.zig
    - 10+ tests in search/semantic.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/lsp/client.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 8+
    - `Select-String -Path src/mcp/discovery.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 8+
    - `Select-String -Path src/search/semantic.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 10+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests read production types but don't modify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | I | test data | accept | No real API keys or server URLs in test data |
</threat_model>

<verification>
1. `zig build` succeeds with no errors
2. 50+ total new tests across 6 files
3. Every file has at least 8 tests
4. No production code changes (tests only)
</verification>

<success_criteria>
- 50+ new tests across 6 AI/infra layer files
- All tests pass
- Build clean
- No production code changes
</success_criteria>

<output>
After completion, create `.planning/phases/68-lsp-mcp-ai-tests/68-01-SUMMARY.md`
</output>
