# Plan: Phase 12 - Config Enhancement

**Phase:** 12
**Goal:** Advanced config with file watching, backups, migrations
**Created:** 2026-04-11

---

## Context

### Research Findings

From reference analysis:

1. **Claude Code** has advanced config:
   - Global/project config scopes
   - File watching for changes
   - Backup and recovery
   - Config migrations

### Current State

Crushcode has:
- ✅ Basic config in `src/config/config.zig`
- ⬜ File watching (not implemented)
- ⬜ Backups (not implemented)
- ⬜ Migrations (not implemented)

### Dependencies

- Phase 1 (Core Infrastructure)

---

## Implementation

### Step 1: Config Scopes

Implement global and project config scopes.

**Files to modify:**
- `src/config/config.zig` - Add scopes

**Changes:**
- Add `GlobalConfig` and `ProjectConfig`
- Support hierarchy (project overrides global)
- Environment variable integration

**Verification:**
- Config loads with correct precedence
- Project config overrides global

---

### Step 2: File Watching

Monitor config file for changes.

**Files to create:**
- `src/config/watcher.zig` - New file

**Changes:**
- Use `std.fs.Watcher` (if available) or polling
- Detect file modifications
- Trigger reload on change

**Verification:**
- Changes detected within reasonable time
- Config reloads automatically

---

### Step 3: Backup System

Create backups before dangerous changes.

**Files to modify:**
- `src/config/config.zig` - Add backup logic

**Changes:**
- Create backup before writes
- Store in versioned files
- Limit backup count

**Verification:**
- Backup created before changes
- Backups can be restored

---

### Step 4: Migration System

Handle config version upgrades.

**Files to create:**
- `src/config/migrate.zig` - New file

**Changes:**
- Define migration functions
- Apply migrations sequentially
- Track config version

**Verification:**
- Migrations apply on upgrade
- Version tracked correctly

---

## Success Criteria (what must be TRUE)

1. [ ] Config changes are watched and reloaded
2. [ ] Backups exist before dangerous changes
3. [ ] Migrations handle version upgrades
4. [ ] Build succeeds with no LSP errors

---

## Notes

- Use Zig stdlib only
- File watching may need polling approach

---

*Plan created from GSD discuss-phase output*