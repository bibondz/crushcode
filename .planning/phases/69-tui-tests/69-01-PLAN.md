---
phase: 69-tui-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/model/streaming.zig
  - src/tui/widgets/multiline_input.zig
  - src/tui/widgets/messages.zig
  - src/tui/widgets/sidebar.zig
  - src/agent/parallel.zig
autonomous: true
requirements:
  - REL-06

must_haves:
  truths:
    - "streaming.zig has tests for finish_reason handling, compaction retry, message formatting"
    - "multiline_input.zig has tests for text buffer operations, cursor movement"
    - "messages.zig has tests for message formatting and rendering helpers"
    - "sidebar.zig has tests for layout calculations"
    - "parallel.zig has tests for AgentCategory enum and model mapping"
    - "All tests pass with zig build"
  artifacts:
    - path: "src/tui/model/streaming.zig"
      provides: "Streaming model test coverage"
      contains: 'test "'
    - path: "src/tui/widgets/multiline_input.zig"
      provides: "Multiline input test coverage"
      contains: 'test "'
    - path: "src/tui/widgets/messages.zig"
      provides: "Messages widget test coverage"
      contains: 'test "'
    - path: "src/tui/widgets/sidebar.zig"
      provides: "Sidebar widget test coverage"
      contains: 'test "'
    - path: "src/agent/parallel.zig"
      provides: "Parallel agent test coverage"
      contains: 'test "'
  key_links: []

---

<objective>
Add test coverage to 5 TUI/agent files. TUI testing is inherently harder (requires terminal mock), so focus on pure-logic extraction: message formatting, buffer operations, layout math, enum logic.

Target: 30+ new tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- TUI/agent layer contracts the executor needs -->

### src/tui/model/streaming.zig (841L, 1 test)
Key testable logic:
- finish_reason handling: "stop", "length", "tool_calls", null
- Compaction retry count tracking
- Message formatting helpers
- Streaming state machine transitions
- Content accumulation logic

### src/tui/widgets/multiline_input.zig (794L, 1 test)
Key testable logic:
- Text buffer operations: insert, delete, backspace at cursor position
- Cursor movement: up/down/left/right, home/end, word navigation
- Line counting and wrapping
- Selection range calculation
- History navigation: up/down through history entries
- Clipboard operations

### src/tui/widgets/messages.zig (502L, 0 tests)
Key testable logic:
- Message formatting: role labels, timestamps
- Content truncation for display
- Code block detection and language extraction
- Markdown formatting helpers
- Message ID generation

### src/tui/widgets/sidebar.zig (484L, 2 tests)
Key testable logic:
- Layout width calculation: min/max bounds, responsive formula
- Item height calculation
- Visible range calculation for virtual scrolling
- Selection state management
- Category filtering

### src/agent/parallel.zig (472L, 0 tests)
Key testable logic:
- `AgentCategory` enum: mapping category to model selection
- Task distribution logic
- Result aggregation
- Priority ordering
- Category determination from task description
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to streaming.zig and multiline_input.zig</name>
  <files>src/tui/model/streaming.zig, src/tui/widgets/multiline_input.zig</files>
  <read_first>
    - src/tui/model/streaming.zig (full file — 841L)
    - src/tui/widgets/multiline_input.zig (full file — 794L)
  </read_first>
  <action>
Read BOTH files completely before writing tests. Focus on extracting and testing pure-logic functions.

**streaming.zig** (1 test → target 8+ new tests):
Find and test pure-logic helpers:
- Test finish_reason parsing: "stop" → stop, "length" → length (critical for Phase 60), "tool_calls" → tool_calls, null/empty → none
- Test compaction retry count: starts at 0, increments, caps at max
- Test message content formatting: role prefix, timestamp format
- Test streaming state transitions (if state enum exists)
- Test content accumulation: empty → has content → more content appended
- Test token counting helpers
- If most functions require terminal context, extract small testable helpers into inline test blocks

**multiline_input.zig** (1 test → target 8+ new tests):
Find and test pure buffer/logic operations:
- Test text insertion at cursor position: empty buffer, middle of text, end of text
- Test character deletion: backspace at start (no-op), middle, end
- Test cursor position tracking after insert/delete
- Test line counting: empty → 1, single line → 1, "hello\nworld" → 2
- Test word boundary detection: for word-jump operations
- Test history navigation: empty history, single entry, multiple entries
- Test selection range: start/end calculation
- If the text buffer is a struct, test its methods directly

IMPORTANT:
- TUI files may import vaxis or terminal types — the test blocks only need to import the file's own types
- If a function requires a Widget or TUI context, test the underlying pure-logic helper instead
- Use `std.testing.allocator` for any buffer allocations
- For multiline_input: look for a `Buffer` or `TextBuffer` struct — test that directly
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 8+ new tests in streaming.zig (total 9+)
    - 8+ new tests in multiline_input.zig (total 9+)
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/tui/model/streaming.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 9+
    - `Select-String -Path src/tui/widgets/multiline_input.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 9+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add tests to messages.zig, sidebar.zig, parallel.zig</name>
  <files>src/tui/widgets/messages.zig, src/tui/widgets/sidebar.zig, src/agent/parallel.zig</files>
  <read_first>
    - src/tui/widgets/messages.zig (full file — 502L)
    - src/tui/widgets/sidebar.zig (full file — 484L)
    - src/agent/parallel.zig (full file — 472L)
  </read_first>
  <action>
Read ALL THREE files completely before writing tests.

**messages.zig** (502L, 0 tests → target 6+ tests):
Find and test pure-logic helpers:
- Test message role formatting: "user" → "You:", "assistant" → "AI:"
- Test timestamp formatting: epoch → readable string
- Test code block detection: "```zig\ncode\n```" → language="zig", has_code=true
- Test content truncation: long content → truncated with "..."
- Test message ordering/ID generation
- Test markdown element detection: headers, lists, links

**sidebar.zig** (484L, 2 tests → target 6+ new tests):
Find and test pure-logic helpers:
- Test layout width: min(30, max(20, width/4)) formula with various widths
- Test auto-hide: width < 80 → hidden
- Test visible range calculation for virtual scrolling: total_items, viewport_height, scroll_offset
- Test item height calculation
- Test selection wrapping: last item → next wraps to first
- Test category filtering

**parallel.zig** (472L, 0 tests → target 6+ tests):
Find and test pure-logic operations:
- Test AgentCategory enum: all values, toString for each
- Test category → model mapping: each category maps to a specific model string
- Test task distribution: N tasks across M categories
- Test result aggregation: merge results from parallel agents
- Test priority ordering: high > medium > low
- Test category determination: "fix the bug" → fix category, "implement X" → implementation

IMPORTANT:
- messages.zig and sidebar.zig are TUI widgets — find pure helpers that don't need terminal context
- parallel.zig should be fully testable (no terminal dependency)
- Use `std.testing.allocator` for any allocations
- For struct methods that need complex init, construct minimal test instances
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 6+ tests in messages.zig
    - 6+ new tests in sidebar.zig (total 8+)
    - 6+ tests in parallel.zig
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/tui/widgets/messages.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 6+
    - `Select-String -Path src/tui/widgets/sidebar.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 8+
    - `Select-String -Path src/agent/parallel.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 6+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests read production types but don't modify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | test data | accept | No real user content in test data |
</threat_model>

<verification>
1. `zig build` succeeds with no errors
2. 30+ total new tests across 5 TUI/agent files
3. Every file has at least 6 tests
4. No production code changes (tests only)
</verification>

<success_criteria>
- 30+ new tests across 5 TUI/agent files
- All tests pass
- Build clean
- No production code changes
</success_criteria>

<output>
After completion, create `.planning/phases/69-tui-tests/69-01-SUMMARY.md`
</output>
