---
phase: 39-forge-naming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/registry.zig
  - src/commands/handlers.zig
  - FORGE_GLOSSARY.md
  - CRUSH.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02

must_haves:
  truths:
    - "Forge alias 'forge' dispatches to the crush command handler"
    - "Forge alias 'strike' dispatches to the chat command handler"
    - "All 17 Forge aliases are registered and dispatch correctly"
    - "Original commands still work unchanged"
    - "Help text mentions Forge aliases"
    - "FORGE_GLOSSARY.md documents all alias mappings"
  artifacts:
    - path: "src/cli/registry.zig"
      provides: "Forge alias entries in comptime command table"
      contains: "forge"
    - path: "FORGE_GLOSSARY.md"
      provides: "Forge terminology reference"
      contains: "Forge Alias"
    - path: "CRUSH.md"
      provides: "Naming conventions for Forge aliases"
      contains: "Forge"
  key_links:
    - from: "src/cli/registry.zig"
      to: "src/commands/handlers.zig"
      via: "handler function references"
      pattern: "handlers\\.handle"
---

<objective>
Add Forge-themed command aliases to the CLI dispatch system, update help text, and create reference documentation.

Purpose: Give Crushcode a unique brand identity through thematic blacksmith/forge naming for all CLI commands. Original commands remain unchanged — Forge names are pure aliases.

Output: 17 Forge aliases in registry.zig, updated help text, FORGE_GLOSSARY.md, CRUSH.md
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Command system contracts the executor needs. Extracted from codebase. -->

From src/cli/registry.zig:
```zig
pub const HandlerFn = *const fn (args_mod.Args, *config_mod.Config) anyerror!void;

pub const Command = struct {
    name: []const u8,
    handler: HandlerFn,
    description: []const u8,
};

// Comptime command table — add entries here for O(1) lookup
const commands = [_]Command{
    .{ .name = "chat", .handler = handlers.handleChat, .description = "Start interactive chat session (streaming)" },
    .{ .name = "crush", .handler = wrapArgsOnly(handlers.handleCrush), .description = "Auto-agentic: plan → execute → verify → commit" },
    // ... all other commands
};

// Dispatch uses std.StaticStringMap for O(1) lookup
pub fn dispatch(command: []const u8, args: args_mod.Args, config: *config_mod.Config) !void {
    const cmd = command_map.get(command) orelse return error.CommandNotFound;
    return cmd.handler(args, config);
}
```

From src/commands/handlers.zig (help text format):
```zig
pub fn printHelp() !void {
    stdout_print(
        \\Crushcode - AI Coding Assistant
        \\
        \\Usage:
        \\  crushcode [command] [options]
        \\
        \\Commands:
        \\  chat           Start interactive chat session (streaming)
        \\  ...
    , .{});
}
```

Handler functions available in handlers.zig:
- `handlers.handleChat` — chat (needs Args + Config)
- `wrapArgsOnly(handlers.handleRead)` — read
- `wrapArgsOnly(handlers.handleShell)` — shell
- `wrapArgsOnly(handlers.handleWrite)` — write
- `wrapArgsOnly(handlers.handleEdit)` — edit
- `wrapArgsOnly(handlers.handleGit)` — git
- `wrapArgsOnly(handlers.handleSkill)` — skill
- `handlers.handleTUI` — tui (needs Args + Config)
- `wrapArgsOnly(handlers.handleTools)` — tools
- `wrapArgsOnly(handlers.handleAgents)` — agents
- `wrapArgsOnly(handlers.handleGrep)` — grep
- `wrapArgsOnly(handlers.handleUsage)` — usage
- `wrapArgsOnly(handlers.handleCheckpoint)` — checkpoint
- `wrapArgsOnly(handlers.handleMCP)` — mcp
- `wrapArgsOnly(handlers.handleSessions)` — sessions
- `wrapArgsOnly(handlers.handleDiff)` — diff
- `wrapArgsOnly(handlers.handleScaffold)` — scaffold
- `wrapArgsOnly(handlers.handleCrush)` — crush
- `wrapArgsOnly(handlers.handleRead)` — read (sparks)

NOTE: Handlers wrapped with `wrapArgsOnly` take only Args. Handlers without wrapper (handleChat, handleTUI) take Args + Config directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 17 Forge aliases to registry.zig command table</name>
  <files>src/cli/registry.zig</files>
  <read_first>
    - src/cli/registry.zig (full file — see current command table structure)
  </read_first>
  <action>
Add the following 17 Forge alias entries to the `commands` array in `src/cli/registry.zig`. Each entry reuses the same handler as its mapped command. Place them AFTER the existing command entries but BEFORE the `help`/`--help`/`-h`/`version`/`--version`/`-v` entries (line ~91-96 area).

The Forge alias mapping:

```zig
// --- Forge naming aliases ---
.{ .name = "forge", .handler = wrapArgsOnly(handlers.handleCrush), .description = "Forge alias: auto-agentic (→ crush)" },
.{ .name = "strike", .handler = handlers.handleChat, .description = "Forge alias: chat session (→ chat)" },
.{ .name = "furnace", .handler = handlers.handleTUI, .description = "Forge alias: terminal UI (→ tui)" },
.{ .name = "alloy", .handler = wrapArgsOnly(handlers.handleSkill), .description = "Forge alias: run skill (→ skill)" },
.{ .name = "anvil", .handler = wrapArgsOnly(handlers.handleEdit), .description = "Forge alias: edit file (→ edit)" },
.{ .name = "bellows", .handler = wrapArgsOnly(handlers.handleShell), .description = "Forge alias: shell command (→ shell)" },
.{ .name = "blueprint", .handler = wrapArgsOnly(handlers.handleScaffold), .description = "Forge alias: project scaffold (→ scaffold)" },
.{ .name = "ledger", .handler = wrapArgsOnly(handlers.handleUsage), .description = "Forge alias: token usage (→ usage)" },
.{ .name = "smiths", .handler = wrapArgsOnly(handlers.handleAgents), .description = "Forge alias: spawn agents (→ agents)" },
.{ .name = "rack", .handler = wrapArgsOnly(handlers.handleTools), .description = "Forge alias: list tools (→ tools)" },
.{ .name = "tongs", .handler = wrapArgsOnly(handlers.handleGrep), .description = "Forge alias: pattern search (→ grep)" },
.{ .name = "reheat", .handler = wrapArgsOnly(handlers.handleSessions), .description = "Forge alias: manage sessions (→ sessions)" },
.{ .name = "slag", .handler = wrapArgsOnly(handlers.handleDiff), .description = "Forge alias: compare files (→ diff)" },
.{ .name = "sparks", .handler = wrapArgsOnly(handlers.handleRead), .description = "Forge alias: read file (→ read)" },
.{ .name = "smelt", .handler = wrapArgsOnly(handlers.handleWrite), .description = "Forge alias: write file (→ write)" },
.{ .name = "quench", .handler = wrapArgsOnly(handlers.handleCheckpoint), .description = "Forge alias: checkpoints (→ checkpoint)" },
.{ .name = "foundry", .handler = wrapArgsOnly(handlers.handleMCP), .description = "Forge alias: MCP tools (→ mcp)" },
```

IMPORTANT:
- Use `handlers.handleChat` (NOT wrapArgsOnly) for "strike" because handleChat takes (Args, *Config)
- Use `handlers.handleTUI` (NOT wrapArgsOnly) for "furnace" because handleTUI takes (Args, *Config)
- Use `wrapArgsOnly(handlers.handle...)` for all others since they only take Args
- Do NOT remove or modify any existing command entries
- The comptime `StaticStringMap` auto-generates the lookup from the `commands` array — no separate registration needed
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 17 new Forge alias entries added to commands array in registry.zig
    - Build succeeds with `zig build`
    - Original commands unchanged
  </done>
  <acceptance_criteria>
    - `src/cli/registry.zig` contains the string `"forge"` exactly 1 time as a command name entry
    - `src/cli/registry.zig` contains the string `"strike"` exactly 1 time as a command name entry
    - `src/cli/registry.zig` contains the string `"furnace"` exactly 1 time as a command name entry
    - `src/cli/registry.zig` contains all 17 Forge alias names: forge, strike, furnace, alloy, anvil, bellows, blueprint, ledger, smiths, rack, tongs, reheat, slag, sparks, smelt, quench, foundry
    - `zig build` exits with code 0
    - The string `"Forge alias:"` appears exactly 17 times in registry.zig
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update help text + create FORGE_GLOSSARY.md and CRUSH.md</name>
  <files>src/commands/handlers.zig, FORGE_GLOSSARY.md, CRUSH.md</files>
  <read_first>
    - src/commands/handlers.zig (lines 179-291 — current printHelp function)
  </read_first>
  <action>
**Part A: Update help text in handlers.zig**

In the `printHelp()` function (starting at line 179), add a Forge Aliases section AFTER the existing `\\Examples:` section (around line 271) but BEFORE the closing `\\` + `.{}` line. Insert:

```
\\
\\Forge Aliases (thematic blacksmith names):
\\  forge          → crush     (auto-agentic)
\\  strike         → chat      (strike up a conversation)
\\  furnace        → tui       (the hot main interface)
\\  alloy          → skill     (blended skills)
\\  anvil          → edit      (shape your code)
\\  bellows        → shell     (pump commands)
\\  blueprint      → scaffold  (plan your build)
\\  ledger         → usage     (track your costs)
\\  smiths         → agents    (many workers)
\\  rack           → tools     (tool storage)
\\  tongs          → grep      (grasp patterns)
\\  reheat         → sessions  (revisit old work)
\\  slag           → diff      (see what's removed)
\\  sparks         → read      (knowledge ignites)
\\  smelt          → write     (extract and create)
\\  quench         → checkpoint (preserve state)
\\  foundry        → mcp       (external tool forge)
\\
```

Also update the header line from `\\Crushcode - AI Coding Assistant` to `\\Crushcode - AI Coding Assistant (Forge Edition)`.

**Part B: Create FORGE_GLOSSARY.md**

Create `/mnt/d/crushcode/FORGE_GLOSSARY.md` with the complete reference documentation:

```markdown
# Forge Glossary

Crushcode's Forge naming system provides thematic blacksmith/forge aliases for all major CLI commands. Original command names continue to work — Forge names are pure aliases.

## Command Aliases

| Forge Alias | Command | Description |
|-------------|---------|-------------|
| `forge` | `crush` | Auto-agentic: plan → execute → verify → commit |
| `strike` | `chat` | Start interactive chat session |
| `furnace` | `tui` | Launch terminal UI |
| `alloy` | `skill` | Run a skill command |
| `anvil` | `edit` | Edit or create a file |
| `bellows` | `shell` | Execute shell command |
| `blueprint` | `scaffold` | Project scaffolding |
| `ledger` | `usage` | Show token usage and costs |
| `smiths` | `agents` | Spawn multiple AI agents |
| `rack` | `tools` | List, enable, disable tools |
| `tongs` | `grep` | AST-grep pattern search |
| `reheat` | `sessions` | Manage chat sessions |
| `slag` | `diff` | Compare two files |
| `sparks` | `read` | Read file content |
| `smelt` | `write` | Write content to file |
| `quench` | `checkpoint` | Manage checkpoints |
| `foundry` | `mcp` | MCP tools management |

## Usage

```bash
# Original commands still work
crushcode chat --provider openai
crushcode tui
crushcode edit src/main.zig

# Forge aliases do the same thing
crushcode strike --provider openai
crushcode furnace
crushcode anvil src/main.zig
```

## Philosophy

The Forge naming system reflects the craft of software development:
- **Forging** is building — transforming raw material (code) into something useful
- **Striking** is engaging — each interaction shapes the result
- **Quenching** is preserving — saving your work at the right moment
- **Smelting** is creating — extracting value from raw input
```

**Part C: Create CRUSH.md**

Create `/mnt/d/crushcode/CRUSH.md` with naming conventions:

```markdown
# CRUSH Naming Conventions

## Forge Alias System

Crushcode uses a **dual-name** command system:
- **Canonical names**: `chat`, `read`, `write`, `edit`, etc. — always work
- **Forge aliases**: `strike`, `sparks`, `smelt`, `anvil`, etc. — thematic alternatives

### Rules

1. **Never remove canonical names** — Forge aliases are additions, not replacements
2. **Forge aliases map 1:1** to canonical commands — no new behavior
3. **Help text shows both** — users discover Forge names naturally
4. **Description format**: `"Forge alias: {purpose} (→ {canonical})"` in registry

### Adding New Forge Aliases

When adding a new CLI command, optionally add a Forge alias:
1. Add canonical command entry to `src/cli/registry.zig` `commands` array
2. Add Forge alias entry with same handler, description format: `"Forge alias: {purpose} (→ {canonical})"`
3. Update FORGE_GLOSSARY.md table
4. Update help text in `src/commands/handlers.zig` `printHelp()`

### Forge Name Selection Criteria

Good Forge names relate to blacksmithing/metallurgy:
- Verbs for actions: `strike`, `smelt`, `quench`
- Nouns for objects: `anvil`, `furnace`, `bellows`
- Concepts: `forge`, `alloy`, `blueprint`

Avoid names that are:
- Already used as canonical commands
- Too similar to existing Forge aliases
- Ambiguous (e.g., `heat` could mean many things)
```
  </action>
  <verify>
    <automated>cd /mnt/d/crushcode && zig build 2>&1 | tail -5 && test -f FORGE_GLOSSARY.md && echo "FORGE_GLOSSARY.md exists" && test -f CRUSH.md && echo "CRUSH.md exists"</automated>
  </verify>
  <done>
    - Help text in handlers.zig includes Forge Aliases section with all 17 mappings
    - Help header shows "Forge Edition"
    - FORGE_GLOSSARY.md exists with complete alias table and usage examples
    - CRUSH.md exists with naming conventions and Forge alias rules
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `src/commands/handlers.zig` contains the string `"Forge Aliases"`
    - `src/commands/handlers.zig` contains the string `"Forge Edition"`
    - `FORGE_GLOSSARY.md` contains `"forge"` and `"strike"` and `"furnace"`
    - `FORGE_GLOSSARY.md` contains all 17 Forge alias names
    - `CRUSH.md` contains `"Forge alias"` and `"Naming Conventions"`
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CLI args | User input via command line (already handled by args.zig parser) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | S | registry.zig | accept | No user-generated content in command names — comptime-only |
| T-39-02 | I | FORGE_GLOSSARY.md | accept | Static documentation, no runtime impact |
</threat_model>

<verification>
1. `zig build` succeeds with no errors
2. `grep -c "Forge alias:" src/cli/registry.zig` returns 17
3. `FORGE_GLOSSARY.md` exists and contains all 17 alias names
4. `CRUSH.md` exists with naming conventions
5. Help text includes Forge Aliases section
</verification>

<success_criteria>
- 17 Forge aliases registered in comptime command table
- All 17 aliases dispatch to correct handlers via StaticStringMap
- Original commands unchanged and functional
- Help text displays Forge aliases section
- FORGE_GLOSSARY.md and CRUSH.md created with complete documentation
- `zig build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/39-forge-naming/39-01-SUMMARY.md`
</output>
