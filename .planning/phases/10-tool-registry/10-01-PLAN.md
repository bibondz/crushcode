# Plan: Phase 10 - Tool Registry

**Phase:** 10
**Goal:** Dynamic tool loading with feature flags and aliases
**Created:** 2026-04-11

---

## Context

### Research Findings

From reference analysis:

1. **Claude Code** has tool registry:
   - Feature-flag controlled loading
   - Tool aliases for compatibility
   - Concurrency safety flags

2. **CheetahCLAWS** has tool registry:
   - `ToolDef` dataclass with function pointers
   - Dynamic registration
   - Schema-based validation

3. **All references** use HashMap/Map for registry

### Current State

Crushcode has:
- Basic tool handling in plugin system
- ⬜ Dynamic registry (not implemented)

### Dependencies

- Phase 6 (MCP Integration)

---

## Implementation

### Step 1: Tool Definition

Define tool types and registry structure.

**Files to create:**
- `src/tools/registry.zig` - New file

**Changes:**
- Define `Tool` struct with call, schema, permissions
- Define `ToolRegistry` with HashMap storage
- Add feature flag support

**Verification:**
- Build succeeds
- Registry initializes correctly

---

### Step 2: Dynamic Loading

Implement runtime tool loading.

**Files to modify:**
- `src/tools/registry.zig` - Add loading logic

**Changes:**
- Load tools from config
- Support feature flags to enable/disable
- Handle tool aliases

**Verification:**
- Tools load at runtime
- Feature flags work correctly

---

## Success Criteria (what must be TRUE)

1. [ ] Tools load dynamically at runtime
2. [ ] Feature flags enable/disable tools
3. [ ] Aliases provide backward compatibility
4. [ ] Build succeeds with no LSP errors

---

## Notes

- Use Zig stdlib only (std.StringHashMap)
- Follow Claude Code's conditional loading pattern

---

*Plan created from GSD discuss-phase output*