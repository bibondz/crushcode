---
phase: 67-command-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/write.zig
  - src/commands/connect.zig
  - src/commands/shell.zig
  - src/commands/git.zig
  - src/commands/doctor.zig
  - src/commands/handlers.zig
autonomous: true
requirements:
  - REL-04

must_haves:
  truths:
    - "write.zig has tests for file writing, content validation, overwrite protection"
    - "connect.zig has tests for provider credential validation"
    - "shell.zig has tests for command execution timeout handling"
    - "git.zig has tests for argument construction and output parsing"
    - "doctor.zig has tests for diagnostic checks and config verification"
    - "handlers.zig has tests for command routing and help output"
    - "All tests pass with zig build"
  artifacts:
    - path: "src/commands/write.zig"
      provides: "Write command test coverage"
      contains: 'test "'
    - path: "src/commands/connect.zig"
      provides: "Connect command test coverage"
      contains: 'test "'
    - path: "src/commands/shell.zig"
      provides: "Shell command test coverage"
      contains: 'test "'
    - path: "src/commands/git.zig"
      provides: "Git command test coverage"
      contains: 'test "'
    - path: "src/commands/doctor.zig"
      provides: "Doctor command test coverage"
      contains: 'test "'
    - path: "src/commands/handlers.zig"
      provides: "Handler routing test coverage"
      contains: 'test "'
  key_links: []

---

<objective>
Add test coverage to 6 command-layer files (2 already have partial tests). Focus on pure-logic functions: argument parsing, validation, string formatting, and configuration handling. Avoid tests that require I/O or network.

Target: 50+ new tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Command layer contracts the executor needs -->

### src/commands/write.zig (266L, 0 tests)
Key functions to test:
- File content validation logic
- Overwrite protection checks
- Path sanitization
- Content formatting helpers

### src/commands/connect.zig (263L, 1 test)
Key functions to test:
- API key format validation (length, prefix checks)
- Provider name recognition
- Config update string building

### src/commands/shell.zig (247L, 1 test)
Key functions to test:
- Command string splitting/escaping
- Timeout argument parsing
- Output capture mode logic

### src/commands/git.zig (471L, 3 tests)
Key functions to test:
- Git argument construction for status/log/diff/add/commit
- Flag parsing (--oneline, --stat, etc.)
- Branch name validation
- Commit message formatting

### src/commands/doctor.zig (460L, 7 tests)
Key functions to test:
- Provider validation logic
- Config file path resolution
- Version string parsing/comparison
- Binary dependency checking

### src/commands/handlers.zig (291L, 2 tests)
Key functions to test:
- Command routing table lookup
- Help text generation
- Unknown command error handling
- Version flag handling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to write.zig, connect.zig, shell.zig</name>
  <files>src/commands/write.zig, src/commands/connect.zig, src/commands/shell.zig</files>
  <read_first>
    - src/commands/write.zig (full file)
    - src/commands/connect.zig (full file)
    - src/commands/shell.zig (full file)
  </read_first>
  <action>
Read ALL THREE files completely before writing any tests. Understand actual function signatures, parameter types, and return types.

**write.zig** (0 tests → target 8+ tests):
- Test content validation: empty content, very large content, content with special chars
- Test overwrite protection: check if file exists logic
- Test path handling: relative paths, paths with spaces, nested directories
- Test content formatting: line ending normalization, encoding checks
- Read actual helper functions first — test whatever pure-logic functions exist

**connect.zig** (1 test → target 8+ new tests):
- Test API key validation: too short, missing prefix, valid format for each provider
- Test provider name normalization: case insensitive, trimming whitespace
- Test config key construction: provider section names, key names
- Test credential format checks: OpenAI (sk-...), Anthropic (sk-ant-...), etc.

**shell.zig** (1 test → target 6+ new tests):
- Test command string parsing: simple commands, quoted args, pipe chains
- Test timeout value parsing: valid, zero, negative, very large
- Test output mode logic: capture vs streaming
- Test command sanitization: dangerous characters, injection prevention

IMPORTANT:
- Read ACTUAL function signatures before writing tests
- Use `std.testing.allocator` and `defer` for cleanup
- Test pure-logic functions only — avoid I/O in tests
- For functions that take allocator: pass `std.testing.allocator`
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 8+ tests in write.zig
    - 8+ new tests in connect.zig (total 9+)
    - 6+ new tests in shell.zig (total 7+)
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/commands/write.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 8+
    - `Select-String -Path src/commands/connect.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 9+
    - `Select-String -Path src/commands/shell.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 7+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add tests to git.zig, doctor.zig, handlers.zig</name>
  <files>src/commands/git.zig, src/commands/doctor.zig, src/commands/handlers.zig</files>
  <read_first>
    - src/commands/git.zig (full file)
    - src/commands/doctor.zig (full file)
    - src/commands/handlers.zig (full file)
  </read_first>
  <action>
Read ALL THREE files completely before writing any tests.

**git.zig** (3 tests → target 10+ new tests):
- Test argument construction for `git status`: --short, --porcelain, --branch flags
- Test argument construction for `git log`: --oneline, -N count, --format string
- Test argument construction for `git diff`: HEAD, staged, specific file paths
- Test argument construction for `git add`: single file, multiple files, --all flag
- Test argument construction for `git commit`: -m flag, message escaping
- Test branch name validation: valid names, special chars, empty string
- Test commit message formatting: multi-line, quotes, special chars

**doctor.zig** (7 tests → target 8+ new tests):
- Test config file path resolution: with HOME, without HOME, custom path
- Test provider validation: known providers (23), unknown provider
- Test version string parsing: "v1.0.0", "1.0.0", "0.13.0-dev"
- Test binary dependency checking: zig version detection
- Test API key format validation per provider

**handlers.zig** (2 tests → target 8+ new tests):
- Test command routing: valid commands → correct handler, unknown → error/help
- Test help text generation: all commands listed, format consistent
- Test version flag handling: --version, -v
- Test provider listing: --providers flag parsing
- Test model listing: --models <provider> parsing
- Test edge cases: empty args, single flag, multiple flags

IMPORTANT:
- Read ACTUAL function signatures — command files often have helper functions not obvious from public API
- git.zig likely builds argument arrays — test array construction, not git execution
- doctor.zig checks may require mocking — test pure validation logic only
- Use `std.testing.allocator` for all allocations
  </action>
  <verify>
    <automated>zig build 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <done>
    - 10+ new tests in git.zig (total 13+)
    - 8+ new tests in doctor.zig (total 15+)
    - 8+ new tests in handlers.zig (total 10+)
    - Build succeeds
  </done>
  <acceptance_criteria>
    - `Select-String -Path src/commands/git.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 13+
    - `Select-String -Path src/commands/doctor.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 15+
    - `Select-String -Path src/commands/handlers.zig -Pattern '^test "' -SimpleMatch | Measure-Object | Select-Object -ExpandProperty Count` returns 10+
    - `zig build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Tests read production types but don't modify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | I | test data | accept | No real API keys or file paths in test data |
</threat_model>

<verification>
1. `zig build` succeeds with no errors
2. 50+ total new tests across 6 command files
3. Every file has at least 7 tests
4. No production code changes (tests only)
</verification>

<success_criteria>
- 50+ new tests across 6 command-layer files
- All tests pass
- Build clean
- No production code changes
</success_criteria>

<output>
After completion, create `.planning/phases/67-command-tests/67-01-SUMMARY.md`
</output>
